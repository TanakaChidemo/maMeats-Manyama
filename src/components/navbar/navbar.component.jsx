import MeatEmojis from "../logo/logo.component";
const Navbar = () => {
  return (
    <div>
      <MeatEmojis />
      <div>
        <a href="/login">Login</a>
      </div>
    </div>
  );
};

export default Navbar;
